language: python
python:
  - 2.6
  - 2.7
  - 3.1
  - 3.2
before_install:
  - sudo apt-get update
  - sudo apt-get install m4 python-imaging
install:
  - pip install --use-mirrors pytest
  - pip install --use-mirrors .
script:
  - py.test tests
branches:
  except:
    - /^play\/.*$/
