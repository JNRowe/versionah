-- {{ magic }}

module("{{ name }}.version", package.seeall)

dotted = "{{ dotted }}"
hex = {{ hex }}
libtool = "{{ libtool }}"
date = "{{ date }}"
table = {{ "{" }}major={{ major }}, minor={{ minor }}, micro={{ micro }}
{%- if patch %}, patch={{ patch}}{% endif -%}{{ "}" }}
web = "{{ web }}"

